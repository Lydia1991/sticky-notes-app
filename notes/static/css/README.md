# 📌 Sticky Notes Task Manager

A clean, responsive, and functional Sticky Notes application built with **Django** and **Bootstrap 5**. This project allows users to create, view, and delete digital sticky notes with a tactile UI/UX.

## 🚀 Features
- **Full CRUD Functionality**: Create, Read, and Delete notes.
- **Tactile UI**: Custom CSS sticky note styling with hover effects and rotations.
- **Form Validation**: Robust server-side validation using [Django Forms](https://docs.djangoproject.com).
- **Responsive Grid**: Mobile-friendly layout using [Bootstrap 5](https://getbootstrap.com).
- **Automated Testing**: Comprehensive unit tests covering all core use cases.

## 📋 Project Requirements (Task Compliance)
- **Unit Tests**: Located in `notes/tests.py`.
- **Design Diagrams**: Located in the `/diagrams` folder.
- **Environment**: Excluded `venv/` per best practices; use `requirements.txt` for setup.

## ⚙️ Installation & Setup
1. **Clone the repository**:
   ```bash
   git clone https://github.com
   cd "sticky_notes project"
